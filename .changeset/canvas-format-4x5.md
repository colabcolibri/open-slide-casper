---
"@open-slide/core": minor
---

Add per-deck canvas format via `meta.format` (`slide` 1920×1080 or `4x5` 1080×1350) through preview, present mode, and export, with a dev-only format picker that updates existing slides on disk.
