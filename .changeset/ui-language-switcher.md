---
"@open-slide/core": minor
"@open-slide/cli": minor
---

Add an in-UI language switcher (next to the theme toggle) that remembers the choice locally; deprecate `config.locale` and drop the `init` language prompt and `--locale` flag.
