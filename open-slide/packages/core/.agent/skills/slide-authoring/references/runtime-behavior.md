# Runtime behavior (free)

- Home page lists every folder under `slides/`.
- Clicking a slide: thumbnail rail, main page, prev/next, counter.
- Arrow keys / PageUp / PageDown. `F` = fullscreen play.
- Play: Space/→ next, ← prev, Esc exit.
- Hot reload on `index.tsx` edits.
