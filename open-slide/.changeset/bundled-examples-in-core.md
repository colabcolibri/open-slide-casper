---
"@open-slide/core": minor
---

Ship design reference decks under `packages/core/examples/` and resolve them from the package root when `examplesDir` is omitted. Consumers get the Examples sidebar without copying decks into their workspace.
