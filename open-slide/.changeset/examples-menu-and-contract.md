---
"@open-slide/core": minor
---

Add `examplesDir` config, Examples sidebar section, and read-only API guards for design reference decks. Demo examples must pass the authoring contract (`demo-examples-contract` test).
