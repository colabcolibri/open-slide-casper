{
  "name": "meridian-workspace",
  "private": true,
  "description": "Meridian harness root; open-slide lives in ./open-slide",
  "scripts": {
    "dev": "pnpm -C open-slide dev",
    "build": "pnpm -C open-slide build",
    "check": "pnpm -C open-slide check",
    "test": "pnpm -C open-slide test"
  }
}
